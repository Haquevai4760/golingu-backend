version: "3.9"

services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - ENV=development
      - DEBUG=true
    volumes:
      - ./app:/app/app   # Hot-reload in dev (remove in prod)
    restart: unless-stopped
